spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/sugarandtee/config
          username: 625424684@qq.com
          password: 1991sarah@
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka
management:
  endpoints:
    web:
      exposure:
        include: "*"

